import { Character } from '../types/character';
import { render } from './base.js';



export function createCommunications(
  selector = 'body',
  position: InsertPosition = 'beforeend',
  character: Character,
) {
  const template = /*html*/ 'h';
  <div class="communications">
    <p class="text">Una frase que dice alguien</p>
    <img class="picture" src="./assets/no-one.jpg" alt="Nombre y familia del que habla" />
  </div>
  console.log(element);
}

